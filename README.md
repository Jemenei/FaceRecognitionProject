# Face Recognition Access System

Простая система распознавания лиц с графическим интерфейсом на **PyQt5**.
Определяет лицо в реальном времени через веб-камеру и сверяет его с базой, хранящейся в **SQLite**.
Поддерживает добавление новых лиц и окно **Login / Register**.

---

## 📌 Функционал

* Распознавание лиц в реальном времени (dlib + OpenCV)
* Графический интерфейс на PyQt5
* Добавление новых лиц в базу данных
* Хранение эмбеддингов лиц в SQLite (`faces.db`)
* Отдельное окно Login / Register
* Автоматическая загрузка dlib-моделей

---

## 📂 Структура проекта

```
FaceRecognitionProject/
│── main.py               # Главное окно и распознавание
│── auth_window.py        # Окно авторизации (Login/Register)
│── database.py           # Работа с SQLite и лицевыми эмбеддингами
│── dat/                  # Dlib модели (shape predictor + ResNet)
│── faces.db              # База данных лиц
```

---

## 📦 Установка зависимостей

Установи необходимые библиотеки:

```bash
pip install pyqt5
pip install opencv-python
pip install dlib
pip install numpy
```

Если dlib не ставится — установи инструменты разработчика:

```bash
xcode-select --install
```

---

## 🚀 Запуск проекта

```bash
python main.py
```

---

## 💾 О базе данных

Все лица сохраняются в файл:

```
faces.db
```

Каждая запись содержит:

* `name` — имя пользователя
* `encoding` — 128-мерный вектор лица (dlib)
